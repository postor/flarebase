# Transport Model

????????????:????? WebSocket,????? REST?

## ??

| ?? | ???? | ?? |
| --- | --- | --- |
| ??????? | WebSocket | ????????????? |
| ????/???? | WebSocket | ?????????? |
| Custom plugin | WebSocket | ?????? worker,?? callback endpoint |
| SSR / SSG | REST | ????????????? |
| `useSWR` / `useSwr` ???? | REST | ????????? |
| SEO ???? | REST | ?????????? CDN |

## WebSocket-first ???

???????:

1. ?????? WebSocket ???
2. ?????????? WebSocket?
3. ??? SSR ????????? REST?

### ?? WebSocket ???

- `collection().onSnapshot()`
- `doc().onSnapshot()`
- ????
- ????
- ??????
- ??????

### ??

```ts
const posts = db.collection('posts');

const unsubscribe = posts.onSnapshot((change) => {
  if (change.type === 'added') {
    console.log('new post', change.doc);
  }
});
```

## REST-first ?????

REST ?????,??????:

- ??????
- Next.js / Nuxt SSR
- SWR ???
- ????????????

### ??

```ts
const result = await db.namedQuery('list_published_posts', {
  limit: 20,
});
```

????????? React Server Component?`getServerSideProps`?loader ? route handler ?,REST ??????

## ??????? POST callback

?? webhook ???:

1. ???????
2. ??? URL ? POST
3. ????????

???? Flarebase ?????,????????:

- ?????
- ????
- ??????????
- ?? session-scoped ??

?????????:

1. ????? Flarebase
2. ????????????
3. ???????? WebSocket ?????
4. ??????

## ????

### ?? 1: Browser app

- ???? REST
- ?????????? WebSocket

### ?? 2: Next.js + SWR

- SSR: REST
- ??? hydrate ?:
  - ?????? REST + SWR
  - ?????? WebSocket ?? `mutate`

### ?? 3: Plugin-driven workflow

- Client -> Server: WebSocket
- Server -> Plugin: WebSocket
- Plugin -> Server: WebSocket
- Plugin -> Client side effects: ??? session collection ?????????????

## ???

### 1. ????????? REST

??:

- ??????
- ???????????????
- ??????

### 2. ????? HTTP callback server

??:

- ?? Flarebase ??????
- ?????????????
- ?????? session-scoped ??

### 3. ? REST ??????

??:

- ?????
- ?????
- ??????

## ?????

```text
?? SSR / SSG / SWR ?????
  ? -> REST
  ? ->
    ???????????
      ? -> WebSocket
      ? -> REST ? WebSocket ??,???????????

???????????
  ? -> Custom plugin over WebSocket
```
