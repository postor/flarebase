# Client Patterns

?????? Flarebase ???????????

## ?????

- ?????:WebSocket-first
- SSR / SWR:REST-first
- ????:WebSocket ??

## ?? 1: SPA / Client Component

?? Flarebase ??????

### ?????

- ????
- ????
- ????
- session ????

### ??

```tsx
import { FlarebaseProvider, useFlarebase } from '@flarebase/react';
import { useEffect, useState } from 'react';

function Posts() {
  const db = useFlarebase();
  const [posts, setPosts] = useState([]);

  useEffect(() => {
    db.collection('posts').get().then(setPosts);

    return db.collection('posts').onSnapshot((change) => {
      console.log(change);
    });
  }, [db]);

  return <div>{posts.length}</div>;
}
```

## ?? 2: Next.js / Nuxt SSR

SSR ????????? WebSocket ???????? REST named query ???

### ??

```ts
const posts = await db.namedQuery('list_published_posts', { limit: 20 });
```

???????? hydration ??,??? WebSocket ????????

## ?? 3: SWR / useSwr

SWR ????“?? + ?? + ???”,????? REST named query?

### ??

```tsx
import useSWR from 'swr';

function usePublishedPosts(db) {
  return useSWR(
    ['list_published_posts', { limit: 20 }],
    ([name, params]) => db.namedQuery(name, params)
  );
}
```

????????????,?????????? WebSocket ??,????????? `mutate`?

## ?? 4: Session-scoped UI state

??????????????????,????????? session collection?

### ??

```ts
const unsubscribe = db.sessionTable('import_status').onSnapshot((change) => {
  console.log('import status', change);
});
```

???????,????????????????

## React / Vue ????

### React

- Provider ?????? Flarebase client ??
- ?????????
- ? `useSWR` ?????????? REST named query

### Vue

- ?????????? client
- ??????????? client
- ?????????,??????????

## ????

```text
UI component
  -> app hook / composable
  -> Flarebase client
     -> WebSocket for realtime and plugin calls
     -> REST for SSR/SWR reads
```

## ????

### 1. ????????????

??:

- ????????
- ? SSR ???

### 2. ???????? REST

??:

- ???????
- ??????

### 3. ?????????? HTTP

??:

- ?? Flarebase ??? WebSocket ??
- ???????

## ??????

### ????

- SSR: `namedQuery('list_published_posts')`
- Hydration ?: ???? `posts`

### ?? / ??

- ????: WebSocket plugin call
- OTP / ????: session collection

### ????????

- ??: REST named query
- ????: WebSocket ???? `mutate`
